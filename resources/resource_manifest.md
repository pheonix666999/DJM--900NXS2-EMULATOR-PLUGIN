# Resource manifest

No external runtime artwork or proprietary font is currently required. Interface labels are live
text and controls are drawn by the custom JUCE LookAndFeel. The documentation reference is not a
runtime resource and must never be added to application binaries or packages.
